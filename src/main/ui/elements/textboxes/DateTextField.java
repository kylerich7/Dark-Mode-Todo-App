package ui.elements.textboxes;

import javax.swing.*;

//represents a text field for date input
public class DateTextField extends TaskInputTextField {

    // MODIFIES: this
    // EFFECTS: creates a DateTextField with default style properties wit
    public DateTextField() {
        setHorizontalAlignment(SwingConstants.CENTER);
    }
}
